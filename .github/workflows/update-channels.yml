name: Update Channel Cache

on:
  schedule:
    - cron: '0 6 * * 1'   # Every Monday at 06:00 UTC
  workflow_dispatch:       # Allow manual trigger

jobs:
  update-cache:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Snapshot current channel cache
        run: cp channels.json channels_prev.json

      - name: Build channel cache
        run: python scripts/build_channels_sxm.py

      - name: Cache logos
        run: python scripts/cache_logos.py

      - name: Detect lineup changes
        id: changes
        run: |
          if python scripts/detect_channel_changes.py channels_prev.json channels.json --apply-aliases channel_aliases.json --write-removed removed.json > channel_changes.md; then
            echo "significant=false" >> "$GITHUB_OUTPUT"
          else
            echo "significant=true" >> "$GITHUB_OUTPUT"
          fi
        continue-on-error: true

      - name: Clean up orphaned logos
        run: python scripts/cleanup_orphaned_logos.py removed.json

      - name: Open GitHub issue on significant changes
        if: steps.changes.outputs.significant == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          DATE=$(date +%Y-%m-%d)
          echo "" >> channel_changes.md
          echo "Source: [rebrowser/siriusxm-dataset](https://github.com/rebrowser/siriusxm-dataset) (updates daily)" >> channel_changes.md
          gh issue create \
            --title "SiriusXM lineup changed — $DATE" \
            --body "$(cat channel_changes.md)" \
            --label "channel-update"

      - name: Clean up temp file
        run: rm -f channels_prev.json channel_changes.md removed.json

      - name: Commit and push if changed
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add channels.json logos/ channel_aliases.json
          if git diff --staged --quiet; then
            echo "Channel cache unchanged — nothing to commit"
          else
            git commit -m "chore: update SiriusXM channel cache, logos, and aliases"
            git push
          fi
